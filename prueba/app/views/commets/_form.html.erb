<%= form_with(model: commet) do |form| %>
  <% if commet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(commet.errors.count, "error") %> prohibited this commet from being saved:</h2>

      <ul>
        <% commet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :comentario, style: "display: block" %>
    <%= form.text_area :comentario %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
