<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <p style="color: green"><%= notice %></p>
    <%= stylesheet_link_tag 'my_style' %>
    <title>Tu Página Web</title>
</head>
<body>

<header>
        <nav>
            <div class="navbar">
                <div class="logo">Mi Sitio</div>
                <ul class="nav-links">
                    <li><a href="#">Inicio</a></li>
                    <li><a href="#">Acerca de</a></li>
                    <li><a href="#">Servicios</a></li>
                    <li><a href="#">Contacto</a></li>
                </ul>
            </div>
        </nav>
    </header>
    <h1>Bill v2s</h1>
<table class="invoice-items-table">
  <thead>
    <tr>
      <th>FECHA</th>
      <th>CLIENTE</th>
      <th>RUC</th>
      <th>COND VENTA</th>
      <th>VALOR PARCIAL</th>
      <th>TOTAL A PAGAR</th>
      <th>IVA</th>
      <th>TELEFONO</th>
      <th>NOTA REMICION</th>
    </tr>
  </thead>

  <tbody>
    <% @bill_v2s.each do |bill_v2| %>
      <tr>
        <td><%= bill_v2.event_date %></td>
        <td><%= bill_v2.cliente %></td>
        <td><%= bill_v2.ruc %></td>
        <td><%= bill_v2.cond_venta %></td>
        <td><%= bill_v2.valor_parcial %></td>
        <td><%= bill_v2.total_pagar %></td>
        <td><%= bill_v2.iva %></td>
        <td><%= bill_v2.telefono %></td>
        <td><%= bill_v2.nota_remi %></td>
        <td class="invoice-action-links">
          <%= link_to "EDITAR", edit_bill_v2_path(bill_v2), class: 'button'%>
          <%= link_to "VER", bill_v2, class: 'button'%>
          <%= button_to "ELIMINAR", bill_v2, method: :delete, data: { confirm: '¿Estás seguro?' }, class: 'button-e' %>
        
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "New bill v2", new_bill_v2_path, class: 'add-invoice-item-button' %>
</body>

